#include<iostream>
#include<vector>
using namespace std;

/*
*比较两个矩阵是否相等
*if m equals n,return true,else return false.
*/
bool compare(const matrix &m,const matrix &n);
vector<int>::iterator change_val(int m,vector<int>::iterator);


